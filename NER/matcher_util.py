import os
from .patterns import patterns

gaz_path = os.path.dirname(os.path.realpath(__file__))

def add_gaz_to_patterns():

    #@todo:add words from gazetteers to ruler patterns

    return patterns
